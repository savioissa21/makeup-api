# Permite substituir beans nos testes (Resolve o erro do jpaAuditingHandler)
spring.main.allow-bean-definition-overriding=true

# Desativa o cache nos testes para não precisar do Redis rodando
spring.cache.type=none

# Usa H2 (Banco em memória) nos testes se não quiser usar o Docker
# (Opcional, se o Docker estiver rodando, pode remover essas linhas de DB)
# spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect